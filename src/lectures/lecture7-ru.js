/* Лекция 7 RU: Абстрактные классы и интерфейсы (Расширенная версия) */
(function(){
  'use strict';
  var html = `
<div class="lecture-card">
  <h2>Лекция 7: Абстрактные классы и интерфейсы</h2>

  <h3>Абстрактные классы</h3>
  <p><strong>Абстрактный класс</strong> — это класс, который служит "незавершенным" шаблоном для других классов. От него нельзя создать экземпляр напрямую (с помощью <code>new</code>), но от него можно наследоваться.</p>
  <p>Основная идея — определить общий каркас поведения и состояния для группы связанных классов, оставив некоторые детали реализации на усмотрение наследников.</p>
  <ul>
    <li>Объявляется с ключевым словом <code>abstract</code>.</li>
    <li>Может содержать <strong>абстрактные методы</strong> — методы, у которых есть только объявление (сигнатура), но нет тела (реализации).</li>
    <li>Если класс содержит хотя бы один абстрактный метод, он сам должен быть объявлен как <code>abstract</code>.</li>
    <li>Класс-наследник обязан реализовать (переопределить) все абстрактные методы родителя, иначе он тоже должен быть объявлен как <code>abstract</code>.</li>
  </ul>
  <pre><code class="lang-java">// Абстрактный класс, определяющий общую логику для всех животных
public abstract class Animal {
    // Обычный, реализованный метод, общий для всех наследников
    public void breathe() {
        System.out.println("Животное дышит...");
    }

    // Абстрактный метод - каждый наследник должен реализовать его по-своему
    public abstract void makeSound();
}

// Конкретный класс, который реализует абстрактный метод
public class Dog extends Animal {
    @Override
    public void makeSound() {
        System.out.println("Гав-гав!");
    }
}</code></pre>

  <h3>Интерфейсы</h3>
  <p><strong>Интерфейс</strong> — это полностью абстрактная структура, которая определяет "контракт" поведения. Он содержит только сигнатуры методов (и константы). Класс, который <em>реализует</em> (<code>implements</code>) интерфейс, обязуется предоставить реализацию для всех его методов.</p>
  <ul>
    <li>Определяет, <strong>что</strong> класс должен делать, но не <strong>как</strong> он это делает.</li>
    <li>Класс может реализовывать любое количество интерфейсов, что позволяет обойти ограничение Java на одиночное наследование.</li>
    <li>Все методы в интерфейсе по умолчанию являются <code>public abstract</code>.</li>
  </ul>
  <pre><code class="lang-java">public interface Flyable {
    void fly(); // public abstract по умолчанию
}

public interface Swimmable {
    void swim();
}

// Класс Duck реализует оба интерфейса
public class Duck implements Flyable, Swimmable {
    @Override
    public void fly() {
        System.out.println("Утка летит...");
    }

    @Override
    public void swim() {
        System.out.println("Утка плывет...");
    }
}</code></pre>

  <h3>Интерактивный пример: Управление медиаплеером</h3>
  <p>Представим интерфейс <code>Playable</code> с методами <code>play()</code> и <code>stop()</code>. Разные классы, такие как <code>AudioPlayer</code> и <code>VideoPlayer</code>, могут реализовать этот интерфейс по-своему.</p>
  <div class="code-example">
      <pre><code class="lang-java">public interface Playable {
    void play();
    void stop();
}

public class AudioPlayer implements Playable {
    public void play() { /* ... играет музыка ... */ }
    public void stop() { /* ... музыка остановлена ... */ }
}

public class VideoPlayer implements Playable {
    public void play() { /* ... показывает видео ... */ }
    public void stop() { /* ... видео остановлено ... */ }
}</code></pre>
      <div class="interactive-controls">
        <p>Выберите плеер:</p>
        <select class="java-input">
          <option value="audio">Аудиоплеер</option>
          <option value="video">Видеоплеер</option>
        </select>
        <button class="run-java-sim" data-action="play">Play</button>
        <button class="run-java-sim" data-action="stop">Stop</button>
      </div>
      <div class="output">
          <p><strong>Статус плеера:</strong></p>
          <pre class="result-output">Результат: (примерный вывод)</pre>
      </div>
  </div>
  <script>
    (function() {
      const container = document.currentScript.closest('.code-example');
      const select = container.querySelector('select');
      const output = container.querySelector('.result-output');
      
      const actions = {
        audio: {
          play: "🎵 Воспроизводится аудиодорожка...",
          stop: "⏹️ Аудио остановлено."
        },
        video: {
          play: "🎬 Воспроизводится видео...",
          stop: "⏹️ Видео остановлено."
        }
      };

      container.querySelectorAll('.run-java-sim').forEach(btn => {
        btn.addEventListener('click', () => {
          const player = select.value;
          const action = btn.dataset.action;
          output.textContent = actions[player][action];
        });
      });
    })();
  </script>

  <h3>Ключевые различия</h3>
  <p><strong>Абстрактный класс</strong> используется, когда есть сильная связь "is-a" (является) и нужно предоставить общую базовую функциональность. <strong>Интерфейс</strong> используется, когда нужно определить "can-do" (может делать) способность, которую могут иметь классы из разных иерархий.</p>
</div>
`;
  window.__LECTURES_ru_7 = html;
})();
